[package]
name = "risc_rt_os"
version = "0.1.0"
edition = "2021"

[dependencies]
riscv-rt = "0.12"
riscv = "0.11"
panic-halt = "0.2"

[profile.release]
opt-level = "z"
lto = true
codegen-units = 1
panic = "abort"

[profile.dev]
opt-level = "s"
panic = "abort"